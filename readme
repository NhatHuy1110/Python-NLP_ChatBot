Library: 
    - NLTK (Natural Language Toolkit) : dùng để xử lý ngôn ngữ tự nhiên (NLP). Cung cấp công cụ và tài nguyên để xử lý văn bản.
    - Numpy : dùng trong xử lý dữ liệu lớn và các phép toán ma trận phức tạp. Được xây dựng chủ yếu để làm việc với mảng n-dimensional array(ndarrays).
    - Torch : là thư viện mã nguồn mở dành cho ML và xử lý tensor, thiết kế để phát triển các ứng dụng DL(DeepLearning).
    - Random: dùng để random khả năng xuất hiện của các câu trả lời.

Algorithm: 
    - Stemmer: là thuật toán trong thư viện NLTK của python, dùng để xử lý ngôn ngữ tự nhiên (NLP). Nó giúp cắt từ về gốc của nó làm giảm thiểu sự đa dạng của từ và chuẩn hóa dữ liệu văn bản
    VD: input là "running", "runner", "ran" Stemmer -> "run"
    - bag_of_words: dùng để chuyển dữ liệu văn bản thành dạng số (thường dùng trong xử lý ngôn ngữ tự nhiên-NLP).
    - Tensor: Pytorch cung cấp DSA tensor, giống với mảng Numpy. Dùng trong ML và DL, dùng để lưu trữ, xử lý và tính toán dữ liệu

Function:
    - Feed-Forward Neural Network: là một mạng perception nhiều lớp
    - SoftMax: hàm đảm bảo rằng mạng xuất ra một phân phối xác xuất trên các lớp có thể có, cho phép mô hình phân loại đầu vào dựa trên xác xuất cao nhất.

NLP Preprocessing Pipeline:
1. Original sentence: "Is anyone there?"
2. Tokenize
    Result: ["Is", "anyone", "there", "?"]
3. Lower + Stem
    Result: ["is", "anyon", "there", "?"]
4. Exclude punctuation characters
    Result: ["is", "anyon", "there"]
5. Bag of Words
    Result: X [0, 0, 0, 1, 0, 1, 0, 1]


